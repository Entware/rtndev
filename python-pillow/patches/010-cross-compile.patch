--- a/setup.py
+++ b/setup.py
@@ -111,13 +111,13 @@
 
 NAME = 'Pillow'
 PILLOW_VERSION = '3.3.0'
-JPEG_ROOT = None
+JPEG_ROOT = os.environ['STAGING_DIR'] + "/opt/lib", os.environ['STAGING_DIR'] + "/opt/include"
 JPEG2K_ROOT = None
-ZLIB_ROOT = None
+ZLIB_ROOT = os.environ['STAGING_DIR'] + "/opt/lib", os.environ['STAGING_DIR'] + "/opt/include"
 IMAGEQUANT_ROOT = None
-TIFF_ROOT = None
-FREETYPE_ROOT = None
-LCMS_ROOT = None
+TIFF_ROOT = os.environ['STAGING_DIR'] + "/opt/lib", os.environ['STAGING_DIR'] + "/opt/include"
+FREETYPE_ROOT = os.environ['STAGING_DIR'] + "/opt/lib", os.environ['STAGING_DIR'] + "/opt/include"
+LCMS_ROOT = os.environ['STAGING_DIR'] + "/opt/lib", os.environ['STAGING_DIR'] + "/opt/include"
 
 
 class pil_build_ext(build_ext):
@@ -345,16 +345,6 @@
 
         # FIXME: check /opt/stuff directories here?
 
-        # standard locations
-        if not self.disable_platform_guessing:
-            _add_directory(library_dirs, "/usr/local/lib")
-            _add_directory(include_dirs, "/usr/local/include")
-
-            _add_directory(library_dirs, "/usr/lib")
-            _add_directory(include_dirs, "/usr/include")
-            # alpine, at least
-            _add_directory(library_dirs, "/lib")
-
         # on Windows, look for the OpenJPEG libraries in the location that
         # the official installer puts them
         if sys.platform == "win32":
@@ -700,18 +690,6 @@
                                        '-qDEB_HOST_MULTIARCH'], stdout=fp)
             except:
                 return
-        try:
-            if ret >> 8 == 0:
-                fp = open(tmpfile, 'r')
-                multiarch_path_component = fp.readline().strip()
-                fp.close()
-                _add_directory(self.compiler.library_dirs,
-                               '/usr/lib/' + multiarch_path_component)
-                _add_directory(self.compiler.include_dirs,
-                               '/usr/include/' + multiarch_path_component)
-        finally:
-            os.unlink(tmpfile)
-
 
 def debug_build():
     return hasattr(sys, 'gettotalrefcount')
